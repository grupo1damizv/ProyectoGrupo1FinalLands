<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:actionViewClass="android.support.v7.widget.SearchView"
        android:title="Search"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_imprimir"
        android:id="@+id/imprimir" />
    <item android:title="Hacer foto"
        android:id="@+id/camara"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_camara2"
        android:visible="true" />
    <item android:title="Guardar"
        app:showAsAction="always"
        android:id="@+id/guardar"
        android:icon="@drawable/ic_action_guardar"
        android:visible="true"></item>
</menu>